{
  "name": "pipeline-test",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Testes OK\" && exit 0",
    "lint": "echo \"Lint OK\" && exit 0",
    "build": "echo \"Build OK\" && exit 0"
  }
}
